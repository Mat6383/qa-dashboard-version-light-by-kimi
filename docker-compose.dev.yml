# ───────────────────────────────────────────────────────────────────────────────
# QA Dashboard by Kimi 2.0 — Docker Compose DEVELOPMENT
# Hot-reload backend (uvicorn) + frontend (Vite HMR)
# ───────────────────────────────────────────────────────────────────────────────

services:
  # ═════════════════════════════════════════════════════════════════════════════
  # Backend API — Hot reload uvicorn
  # ═════════════════════════════════════════════════════════════════════════════
  backend:
    build:
      context: ./backend_py
      dockerfile: Dockerfile.dev
    container_name: qa-dashboard-backend-dev
    restart: "no"
    env_file:
      - ./backend_py/.env
    environment:
      - ENV=development
      - FRONTEND_URL=http://localhost:3000
    volumes:
      # Bind mount du code source pour le hot-reload
      - ./backend_py:/app
      # Protection de .venv (bindings natifs compilés dans l'image)
      - /app/.venv
      # Persistance SQLite
      - ./backend_py/db-data:/app/db-data
      # Logs host-accessibles
      - ./backend_py/logs:/app/logs
    ports:
      - "3001:3001"
    networks:
      - dashboard-net-dev

  # ═════════════════════════════════════════════════════════════════════════════
  # Frontend — Vite dev server avec HMR
  # ═════════════════════════════════════════════════════════════════════════════
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: qa-dashboard-frontend-dev
    restart: "no"
    environment:
      - NODE_ENV=development
      # Proxy Vite vers le backend dans le réseau Docker
      - BACKEND_URL=http://backend:3001
      # Force le polling de chokidar pour le HMR sous Docker
      - CHOKIDAR_USEPOLLING=true
    volumes:
      # Bind mount du code source pour le hot-reload
      - ./frontend:/app
      # Protection de node_modules
      - /app/node_modules
    ports:
      - "3000:3000"
    networks:
      - dashboard-net-dev
    depends_on:
      - backend

networks:
  dashboard-net-dev:
    driver: bridge
